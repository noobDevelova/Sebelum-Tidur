/**
 * Enum representing the active status of an item.
 *
 * @readonly
 * @enum {string}
 */
export enum ActiveStatus {
  /**
   * Represents the active state.
   */
  Active = 'active',

  /**
   * Represents the inactive state.
   */
  InActive = 'inActive',
}
